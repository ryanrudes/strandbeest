#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Check if the pointer is NULL
 * 
 * This is used to check if memory allocation was successful. If not,
 * it will raise an error.
 */
void check_memory(void *ptr);

#endif // UTILS_H